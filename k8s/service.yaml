apiVersion: v1
kind: Service
metadata:
  labels:
    app: pipelines-vote-api
  name: pipelines-vote-api
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: pipelines-vote-api
